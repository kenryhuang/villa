# 主场景工具取消与作物时间推进实施计划

1. 在控制器与 HUD 测试中加入未选择工具的失败用例。
2. 在季节系统测试中加入 5 分钟日长和推进到下一天的失败用例。
3. 实现 `PlayerActionController.deselect_slot()`、`Esc` 取消和未选择时关闭预览/操作。
4. 让 HUD 正确显示未选择状态。
5. 将主游戏日调整为 300 秒，并实现统一的 `advance_to_next_day()`。
6. 在主场景调试构建中连接 `N` 快捷推进。
7. 运行完整测试、编辑器解析检查和主场景实际启动检查。
