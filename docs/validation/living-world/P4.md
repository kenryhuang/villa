已验收（2026-09-08）

# P4：NPC 筹料、自建与营业

工作区：`feature/3d-farm-preview`，基于 `9462470` 的未提交实现。

直接扩展原 `BuildingSystem` / 3D 建造检查的 actor 参数。NPC 使用自己的位置和材料，不借用玩家位置、库存或蓝图等级。地块预约有期限，检查原地图的地形、占地、保护区与已有预约；玩家也不能抢占 NPC 已预约地块。风车沿用正式 3D 模型、真实占地和原施工阶段，需要木板 12、石砖 8、绳索 2，建成前关闭营业。

项目能够采购、走到地块、预约、建造、等施工、设置自己的费率，随后使用 P1 的收费生产系统。`rent.building_id="@建造步骤ID"` 可引用尚未生成的建筑，避免模型编造实例 ID。原逻辑 `build` 定时器在 3D 中被拒绝，必须走真实建造路径。未完成施工可退款取消；完成后不能退款，合法拆除保留历史记录但不会阻止后续存读档。

专项：`godot_console.exe --headless --path . --script tests/run_living_world_p4_tests.gd -- --farm-test`，**25 项通过**。覆盖缺料、地块争抢（含玩家）、越界、远处 NPC / 近处玩家不能建造、真实扣料与施工、重试不重复建造、施工中重载、取消只退款一次、建成营业、他人不能调价、拆除后恢复。P1 的 60 项产权/收费回归仍通过。

真实 AI：

```powershell
godot_console.exe --headless --path . --script tests/run_living_world_autonomy_live_tests.gd -- --living-world-scenario=P4 --living-world-live-agents
```

已通过。夹具仅设定部分材料、预算、已有风车关闭和旅店真实出资的长期需求，没有提供动作脚本。老李自行制定并完成了 **11 步**：买料、买粮、接委托、移动、预约、建风车、等待、开放收费、使用自有风车加工、等待、交付 20 份面粉。开放费率由模型选择为每批 5 金币。保存重载成功，并再次通过最新校验。证据：忽略目录 `tmp/living-world/P4-autonomy-live.json`、`P4-autonomy-save.json`。

失败观察：第一轮因缺少「引用未来建筑」的明确契约，模型选择询问玩家能否开放风车；补齐步骤结果引用后才完成自建链。P3 的共享资金不足反例也验证了模型没有无资金强推建造。

边界：首个可自主建造/经营的建筑是风车，其他建筑推广在 P8；公共空地首期预约免费，不因此免材料。建筑仍需满足原地形与实际角色距离约束。
