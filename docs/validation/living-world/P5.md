已验收（2026-09-08）

# P5：需求、委托与实际交付

工作区：`feature/3d-farm-preview`，基于 `9462470` 的未提交实现。

市场前侧有木制实体委托牌，随市场存档位置恢复。走近点击，可查看可接委托、我的接单、我的发布并创建新单。采购允许现货，加工要求接单后完成的新生产订单。发布者从自己的账户预付报酬，交付时原子转移真实货物和托管金币；可分批交付，旧版本、超量、重复凭证及到期交货会被拒绝。

同源需求统一记录总量、已满足及委托预留。旅店按已有面包储备计算缺口，最多将 2 份交给公告板，后台采购只能使用其余份额，消费实际扣库存。有正在履行的接单时不能提前撤销；到期退款只退未交付部分并释放需求额度。每天按需求 ID 去重。

玩家可以直接填表，也可在与 NPC 的对话中提出需求：`propose_player_commission` 只生成草稿，关闭对话后呈现完整确认条款，确认才扣钱发布。未确认草稿不占资产，读档或退出可以丢弃。NPC 的发布、接取、交付调用同一领域规则，并可加入自己的项目。

专项：`godot_console.exe --headless --path . --script tests/run_living_world_p5_tests.gd -- --farm-test`，**39 项通过**。覆盖最后名额争抢、后台与委托共享需求、部分/超额/重复交货、条款版本、到期顺序、退款、旧货冒充新加工、生产凭证额度、玩家确认前后资金、对话草稿、关闭面板恢复输入与时钟及存读档。

真实 AI：

```powershell
godot_console.exe --headless --path . --script tests/run_living_world_autonomy_live_tests.gd -- --living-world-scenario=P5 --living-world-live-agents
godot_console.exe --headless --path . --script tests/run_living_world_autonomy_live_tests.gd -- --living-world-scenario=P5 --living-world-live-agents --living-world-draft-check
```

均已通过。老李根据玩家已出资的需求自主接取 2 份面粉加工委托，付加工费、等实际产出、交货并领取 600 金币，保存恢复成功。另一次真实对话生成「2 份面粉、每份 100 金币、600 分钟、最多 1 人、允许现货」草稿，确认前钱包不变，UI 确认后扣 200 金币并发布，随后成功存读档。

图形脚本 `tests/run_living_world_visual_tests.gd` 已运行并检查截图。可重现输出在忽略目录 `tmp/living-world/P5-world-board.png`、`P5-commissions.png`、`P5-publish-confirm.png`。真实模型追踪也保留在该目录，不提交私人对话和运行存档。

边界：当前委托为采购和加工。已有接单的条款不静默改动；新数量/价格需要新确认，未接单可取消再发。运输、复杂多方分账和长期合同后续扩展。

## P2～P5 联合回归

四阶段专项共 **151 项**通过（55 / 32 / 25 / 39），Agent 服务 **48 项**通过。P0/P1 共 85 项及原 Agent 1703、3D Agent 57、生产 407、风车 112、食品工坊 77、存档保护 18 项均通过。生产和存档保护测试保留原有故障注入的预期错误/警告输出。

正式玩法回归：交互 44、目标系统 107、农田 45、市场 103、钓鱼 105、高尔夫 123、小地图 22 项通过。v1/v2/v3 旧存档测试样本已去除 v7 专属 `living_world` 字段，继续验证真实旧格式迁移。这些回归已加入 `run_living_world_tests.ps1 -IncludeDependencies`。P3/P4/P5 真实 AI 完整存档另经最新校验器逐一重载成功。
