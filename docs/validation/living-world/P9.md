# P9：实地调查与私人情报

2026-09-09，当前 3D 分支工作区。已验收：正式场景测试 51 项通过，真实 Agent 验证通过，相关跨阶段回归通过。

原知识注册表、活动系统和执行器直接扩展。河岸、林地、丘陵调查要求真实可达、实际到场、面包补给及20游戏分钟。每日地点的有限样本由保存的种子决定；同一地点每日不能重复产出，调查也可能没有发现。资助协议由两方确认，托管2份面包和约定报酬；实际返程交报告后结算。是否要求新报告、是否必须现场样本分别写入条款；购买样本不适用于现场采样条款。

情报报价只显示概要，付款成功才授权报告与地图标记；免费分享同步知识状态，已知或公开的事实不能再收费。调查活动的内部坐标、证据不进入公共行动事件。存档检查报告的原活动凭据、样本唯一性、资助守恒及幂等键的主体和参数。

命令：

```powershell
godot_console.exe --headless --path . --script tests/run_living_world_p9_tests.gd -- --farm-test
godot_console.exe --headless --path . --script tests/run_living_world_p9_live_tests.gd -- --living-world-scenario=P9 --living-world-live-agents
```

最新真实模型验证：资助60金币、明确 `allow_old_report=false`，实际出发、调查、返程交付；另以30金币出售丘陵私人报告，付款后解锁位置。一次先前运行只口头提议免费分享、没有提交授权命令，按失败保存在 `P9-live-no-paid-offer.json`；没有将口头承诺强行成交。最新成功记录为 `P9-live.json`。

本机证据在忽略目录 `tmp/living-world/P9-live*.json`，正式存档未读取或改写。模型能拒绝或选择免费分享，单次成功不是每次必定成交的承诺。普通实地报告属于观察；P10天气预报独立标明预测性质，不当作已发生的天气事实。

知识视图区分观察、证据、公开事实、传闻和预测。对话记录为带来源、期限的传闻；只有调查凭据能支撑事实。明确分享或逐字完整披露已知报告会免费授权；尚未实现通用自然语言事实抽取，不能保证识别所有改写的隐含泄露。
