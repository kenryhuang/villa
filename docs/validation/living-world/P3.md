已验收（2026-09-08）

# P3：自主项目与持续执行

工作区：`feature/3d-farm-preview`，基于 `9462470` 的未提交实现。

原 Agent 服务、角色能力、动作验证和路由接入 `submit_project` / `retry_project` / `cancel_project` / `suggest_behavior`。项目有自由文本目标、预算、时限、材料与最多 12 步的有向无环图；同一 NPC 只有一个主项目。预算与近期材料从原账户转入项目托管。实际购买、租用、销售仍通过原市场与生产事务；成品按原订单事件进入项目库存，结束后返还余款和余料。

步骤与结果引用同时在 Godot / 服务端验证，拒绝循环、未知能力和错误引用。等待加工不请求模型；容量不足由队列事件唤醒后重验，价格不合意保留成品并阻塞，模型可取消未承诺部分后另立项目。已有项目执行期间抑制重复规划。临时建议有期限，进入后续决策上下文；承诺中的订单保留，复杂临时插单在 P6。

专项：`godot_console.exe --headless --path . --script tests/run_living_world_p3_tests.gd -- --farm-test`，**32 项通过**。覆盖预算超额、幂等提交、实际采购/排产/售出、进行中及结束后读档、价格阻塞无重复尝试、容量释放事件、未开工订单取消与托管退回、建议到期。调试面板显示目标、每步状态、阻碍和余款，后续模型上下文包含近期项目和已接委托。

真实 AI：

```powershell
godot_console.exe --headless --path . --script tests/run_living_world_autonomy_live_tests.gd -- --living-world-scenario=P3 --living-world-live-agents
godot_console.exe --headless --path . --script tests/run_living_world_autonomy_live_tests.gd -- --living-world-scenario=P3 --living-world-live-agents --living-world-counterexample
```

均已通过。无玩家指令、无公告板订单的定时请求中，老李自行提交了「自有谷物加工面粉获利，并补齐关键储备」项目，执行加工、等待、出售、购买种子和盐，随后成功保存重载。零金币、没有可租设备及已出资需求的反例中，模型没有强推生产/建造，选择放弃或更小的资金周转目标。原始追踪在忽略目录 `tmp/living-world/P3-autonomy-live*.json`；真实运行存档通过最新版本校验。

保留的失败观察：首轮模型设置的整批最低售价未满足，规则正确阻止出售，保留 8 份面粉；随后补充了整批报价/深度说明、阻塞结果及重新规划路径。模型输出并非保证一次成功。

边界：首批项目加工支持明确原料配方（例如面粉、面包）；带 `input_selectors` 的鱼类替代配方暂不进项目自动链，原直接租用加工仍可用。背景居民不各自调用模型。实时动作继续使用原引擎时间，不能把模型回复视为完成。
